---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default-mtls
  namespace: confluent
spec:
 selector:
   matchLabels:
     confluent-platform: "true"
 mtls:
    mode: STRICT